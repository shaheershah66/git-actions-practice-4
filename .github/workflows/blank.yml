name: Test artifacts

on:
  workflow_dispatch:

jobs:
  job1:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      
      - run: mkdir -p ./build

      - run: cd build

      - run: echo "Hello world" > hello.txt

      - run: ls build
