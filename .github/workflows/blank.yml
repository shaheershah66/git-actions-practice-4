name: Test artifacts

on:
  workflow_dispatch:

jobs:
  job1:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      
      - run: |
          mkdir -p build
          cd build
          echo "Hello world" > hello.txt
          ls
